import RecursiveCopy from './recursive'
import ReplaceCopy from './replace'

namespace Copy {
  export import Recursive = RecursiveCopy // eslint-disable-line
  export import Replace = ReplaceCopy // eslint-disable-line
}

export default Copy
